# For buildspec.yml use only requirements.txt
# For local development, install requirements.txt first, then requirements-local.txt

# For local development, install using a Python virtual environment
# python3 -m venv .venv
# source .venv/bin/activate
# pip install -r requirements.txt
# pip install -r requirements-local.txt

# Processor Lambda function dependencies
# AWS SDK is provided by Lambda runtime, but we can specify versions if needed
boto3>=1.26.0
botocore>=1.29.0

# Additional dependencies specific to processor function
# (none currently needed beyond AWS SDK)