# AWS SDK - already available in Lambda runtime, but included for local testing
boto3==1.34.34
botocore==1.34.34

# Testing frameworks
pytest==7.4.3
pytest-mock==3.12.0
hypothesis==6.92.1

# AWS service mocking for local testing
moto[all]==4.2.9
