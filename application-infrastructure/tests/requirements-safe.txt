# SAFE Testing Dependencies - Reduced crash risk
# Use this file instead of requirements.txt to prevent system crashes

# Core testing frameworks (safe)
pytest==8.3.3
pytest-mock==3.14.0
hypothesis==6.112.1

# AWS SDK (safe - already available in Lambda runtime)
boto3==1.35.36
botocore==1.35.36

# AWS service mocking (DANGEROUS - commented out)
# moto[all]==5.0.18  # This package is 500MB+ and can crash the system
# Only install if absolutely necessary and monitor system resources

# Alternative: Use moto without [all] extras for specific services only
# moto[s3,sqs,cloudfront]==5.0.18  # Smaller, service-specific installation