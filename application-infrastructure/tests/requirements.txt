# For buildspec.yml use only requirements.txt
# For local development, also install requirements-local.txt

# SAFE Testing Dependencies - Reduced crash risk
# Use this file in buildspec instead of requirements-local.txt to speed up deployments

# Core testing frameworks (safe)
pytest==8.3.3
pytest-mock==3.14.0
hypothesis==6.112.1

# AWS SDK (safe - already available in Lambda runtime)
boto3==1.35.36
botocore==1.35.36
